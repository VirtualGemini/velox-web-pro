# 【生产】环境变量

# 应用部署基础路径（如部署在子目录 /admin，则设置为 /admin/）
VITE_BASE_URL = /

# API 地址前缀：留空表示与前端同源（经反向代理把 /api 转发到后端，推荐）；
# 如后端为独立域名，请由 CI/部署注入完整地址（例如 https://api.example.com），切勿提交真实/mock 地址
VITE_API_URL =

# Delete console
VITE_DROP_CONSOLE = true